import { Component } from "react";

class Lists extends Component {
    render() {
        return (<p>This is the Lists Page (where albums you want to listen later and organise are)</p>
        );
    }
}

// Export for use in App.js
export default Lists;