import { Component } from "react";

class Profile extends Component {
    render() {
        return (<div className="content"><p>This is the Profile Page (where user information will be displayed)</p></div>
        );
    }
}

// Export for use in App.js
export default Profile;