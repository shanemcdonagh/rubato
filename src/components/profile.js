import { Component } from "react";

class Profile extends Component {
    render() {
        return (<p>This is the Profile Page (where user information will be displayed)</p>
        );
    }
}

// Export for use in App.js
export default Profile;